@using ServiceDesk.Core.Enums
@model ServiceDesk.Core.Models.SoftwareLicense
@{
    ViewData["Title"] = $"Edit — {Model.ProductName}";
}

<nav aria-label="breadcrumb" class="mb-3">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a asp-action="Index">Software Licenses</a></li>
        <li class="breadcrumb-item active">Edit</li>
    </ol>
</nav>

<h2 class="mb-4"><i class="bi bi-pencil me-2"></i>Edit License — @Model.ProductName</h2>

<div class="card shadow-sm" style="max-width:720px;">
    <div class="card-body">
        <form asp-action="Edit" method="post">
            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="Id" />
            <input type="hidden" asp-for="CreatedDate" />
            @await Html.PartialAsync("_LicenseForm", Model)
            <div class="mt-4 d-flex gap-2">
                <button type="submit" class="btn btn-primary"><i class="bi bi-check-lg me-1"></i>Save Changes</button>
                <a asp-action="Index" class="btn btn-outline-secondary">Cancel</a>
            </div>
        </form>
    </div>
</div>
