{
  "Logging": {
    "LogLevel": {
      "Default": "Warning",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "servicesphere.seamlessflooring.com",

  // -----------------------------------------------------------------------
  // PRODUCTION SECRETS — Do NOT store real values here.
  // Override via environment variables on the server:
  //
  //   ConnectionStrings__ServiceSphere=Server=...;Database=...;User Id=...;Password=...;
  //   App__BaseUrl=https://servicesphere.seamlessflooring.com/
  //
  // In IIS: set via Application Pool > Environment Variables
  // In Azure App Service: set via Configuration > Application Settings
  // In Linux systemd: set in the .service file as Environment= entries
  // -----------------------------------------------------------------------
  
  "ConnectionStrings": {
    "ServiceSphere": "Server=74.114.167.52,1433;Database=ServiceSphere;User Id=sfc;Password=aya;TrustServerCertificate=True;"
  },
  "App": {
    "BaseUrl": "https://localhost:63221/"
  }
}
